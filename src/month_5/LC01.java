package month_5;

public class LC01 {
    //182
    //SELECT distinct p1.Email Email from Person p1 , Person p2 where p1.Email = p2.Email and p1.Id != p2.Id
    //SELECT Email from Person group by Email Having count(*) > 1
}
