package month_5;
//1156. 单字符重复子串的最大长度
public class LC06 {
    /*public int maxRepOpt1(String text) {

        char[] chars = text.toCharArray();
        char[] charCount = new char[26];
        for (char aChar : chars) {
            charCount[aChar-'a']++;
        }
        int max = Integer.MIN_VALUE;
        int first = 0;
        int second = 0;
        char firstChar;
        char secondChar;
        for (int i = 0; i < chars.length; i++) {
            //if(charCount[chars[i]-'a']<=max) break;
            firstChar=chars[i];

        }
    }*/
}
